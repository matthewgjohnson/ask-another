{
  "manifest_version": "0.4",
  "name": "ask-another",
  "display_name": "Ask Another LLM",
  "version": "2.0.2",
  "description": "Query other LLMs through a unified MCP interface",
  "long_description": "Ask Another lets Claude query other LLMs (OpenAI, Gemini, OpenRouter, and more) for second opinions, deep research, image generation, or capabilities beyond Claude's own. Configure providers via API keys; models are discovered automatically. Includes background research jobs, image generation across multiple model families, and personal annotations on models.",
  "author": {
    "name": "Matthew G Johnson",
    "url": "https://github.com/matthewgjohnson"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/matthewgjohnson/ask-another.git"
  },
  "server": {
    "type": "uv",
    "entry_point": "src/ask_another/server.py",
    "mcp_config": {
      "command": "uv",
      "args": [
        "run",
        "--directory", "${__dirname}",
        "src/ask_another/server.py"
      ],
      "env": {
        "PROVIDER_OPENAI": "${user_config.provider_openai}",
        "PROVIDER_GEMINI": "${user_config.provider_gemini}",
        "PROVIDER_OPENROUTER": "${user_config.provider_openrouter}",
        "CACHE_TTL_MINUTES": "360",
        "ZERO_DATA_RETENTION": "${user_config.zero_data_retention}"
      }
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "linux", "win32"],
    "runtimes": {
      "python": ">=3.10"
    }
  },
  "user_config": {
    "provider_openai": {
      "type": "string",
      "title": "OpenAI API Key (optional)",
      "description": "Your OpenAI API key (starts with 'sk-'). Leave blank to skip OpenAI.",
      "sensitive": true,
      "required": false
    },
    "provider_gemini": {
      "type": "string",
      "title": "Gemini API Key (optional)",
      "description": "Your Google AI Studio API key. Leave blank to skip Gemini.",
      "sensitive": true,
      "required": false
    },
    "provider_openrouter": {
      "type": "string",
      "title": "OpenRouter API Key (optional)",
      "description": "Your OpenRouter API key (starts with 'sk-or-'). Leave blank to skip OpenRouter.",
      "sensitive": true,
      "required": false
    },
    "zero_data_retention": {
      "type": "boolean",
      "title": "Zero Data Retention (OpenRouter)",
      "description": "Filter OpenRouter results to ZDR-compatible models only",
      "default": true,
      "required": false
    }
  },
  "keywords": ["llm", "openai", "gemini", "openrouter", "research", "image-generation", "multi-model"],
  "license": "MIT"
}
